**what is difference between Job and step**

Job is the groups of steps, whereas steps are the individual task under one job like "running shell commands" or uses: "uses a prebuilt action".
steps share the same environment, but jobs are isolated.
